datastax-java-driver {

  # We have to provide the credentials for tests.
  # See also https://docs.datastax.com/en/developer/java-driver/4.6/manual/core/authentication/#plain-text
  advanced.auth-provider {
    class = PlainTextAuthProvider
    username = cassandra
    password = cassandra
  }

}
